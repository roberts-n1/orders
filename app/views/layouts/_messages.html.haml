- flash.each do |name, msg|
  %div.mt-4{ class: "alert alert-#{name == 'notice' ? 'success' : 'danger'} alert-dismissible fade show", role: 'alert'}
    = msg
    %button.close{ aria: { label: 'Close' }, data: { dismiss: 'alert' }, type: 'button' }
      %span{ aria: { hidden: 'true' } } ×
